# GIT_USB
A project to develop a system that would allow two or more USB's to have 'Shared Folders' on them which would automatically be synced together using
git version control. Developed in Python using the GitPython to interface with git.

## Still a work in progress ##
